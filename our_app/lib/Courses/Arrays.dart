import 'package:flutter/material.dart';

class ArraysScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Arrays course!"),
      ),
    );
  }
}
