import 'package:flutter/material.dart';

class RepeatloopScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("repaet loop!"),
      ),
    );
  }
}
