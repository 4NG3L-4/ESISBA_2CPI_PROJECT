import 'package:flutter/material.dart';

class PointerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Pointer course"),
      ),
    );
  }
}
