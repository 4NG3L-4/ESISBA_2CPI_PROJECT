import 'package:flutter/material.dart';

class LinkedlistScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Linked_list course !"),
      ),
    );
  }
}
